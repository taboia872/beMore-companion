<resources>

    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:windowBackground">#0d1117</item>
        <!-- Diz ao Android: a area do app NAO se estende sob a status bar nem
             a barra de navegacao. A UI comeca abaixo delas. Sem essa flag,
             Theme.AppCompat.NoActionBar assume edge-to-edge implicito. -->
        <item name="android:windowTranslucentStatus">false</item>
        <item name="android:windowTranslucentNavigation">false</item>
        <item name="android:fitsSystemWindows">true</item>
        <item name="android:statusBarColor">#0d1117</item>
        <item name="android:navigationBarColor">#0d1117</item>
    </style>

</resources>
